<div class="row">
	<div class="span6 offset3">
		<div class="well">
			<%= link_to image_tag(@pin.image), @pin.url, :target => "_blank" %>
<p>
  <%= @pin.description %>
</p>
<% if current_user == @pin.user %>
	<%= link_to 'Edit', edit_pin_path(@pin) %> |
<% end %>
<%= link_to 'Back', pins_path %>
		</div>
	</div>
</div>